# Matrix Dendrite Homeserver

This repository contains a Dendrite Matrix Homeserver implementation, optimized for deployment on Render using Docker.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the development server:
   `npm run dev`
